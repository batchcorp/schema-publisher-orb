description: >
  Publish file descriptor set artifact
usage:
  version: 2.1
  orbs:
    <orb-name>: batchcorp/schema-publisher-orb@1.0.0
  workflows:
    use-my-orb:
      jobs:
        - schema-publisher-orb/publish
